# Описание предметной области:

Библиотека располагает фондом книг, который постоянно пополняется. Книги, находящиеся в библиотеке, изданы различными издательствами. Каждый читатель может взять на абонемент не более пяти книг на срок до 20 дней (включая).
Необходимо спроектировать базу данных БИБЛИОТЕКА, информация которой будет использоваться для получения оперативной информации о наличии книг в библиотеке, наличии книг у читателя, для контроля своевременности возврата книг.

В БД должна храниться информация:

    об ИЗДАТЕЛЬСТВАХ: код издательства, наименование издательства, город;
    КНИГАХ: шифр книги, название книги, первый автор, год издания, цена книги (руб.), количество экземпляров (шт.);
    ЧИТАТЕЛЯХ: код читателя, Ф.И.О. читателя, адрес, телефон;
    ВЫДАЧАХ: код читателя, шифр книги, дата выдачи.

При проектировании БД необходимо учитывать следующее:

    в фонде библиотеки могут храниться несколько книг одного и того же издательства. Книга издается только одним издательством;
    книга может быть затребована несколько раз на выдачу. За одну операцию "ВЫДАЧА" выдается только одна книга;
    читатель может быть задействован в выдаче нескольких книг. Каждая выдача относится к одному читателю.

Кроме того следует учесть:

    каждая книга, находящаяся в фонде библиотеки, обязательно издается издательством. Издательство обязательно издает книги;
    книга не обязательно может быть затребована на выдачу. Каждая выдача обязательно связана с книгой;
    каждый читатель обязательно задействован в выдаче. В каждой выдаче обязательно задействован читатель.
Техническое задание:

    Реализовать базовый CRUD для книг, издательств, читателей и выдачах, а также генерацию отчёта в файл (описание ниже);
    Реализовать отдельный endpoint генерации отчёта api/report?date=2024-05-25 который должен отдавать файл в формате csv/xslx, где date url-параметр для генерации отчёта. (Можно добавить в проект зависимость для работы с xslx).

На доп. баллы:

    Использовать итератор или генератор;
    Реализовать любой unit-тест для бизнес логики приложения (Использовать pytest);
    Реализовать скрипт генерация фикстур в БД для приложения;
    Написать README.md для проекта (описание проекта, как разворачивать для разработки, как тестировать и т.д.);